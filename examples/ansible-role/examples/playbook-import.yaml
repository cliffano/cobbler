---
- name: CobblerExample playbook import
  hosts: localhost

  tasks:

    - name: Provision CobblerExample
      ansible.builtin.import_role:
        name: cliffano.cobblerexample
      vars:
        ans_reverse: true
        ans_transformation: 'upper'
